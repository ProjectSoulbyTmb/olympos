{
  "name": "riley-studio",
  "productName": "RileyStudio",
  "version": "1.0.0",
  "description": "Riley Studio - fully local AI creative suite (canvas, gallery, models)",
  "main": "main.js",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test \"tests/**/*.test.js\"",
    "package": "electron-packager . RileyStudio --platform=win32 --arch=x64 --out=release --overwrite --asar=false --ignore=\"^/release\" --ignore=\"^/node_modules/electron($|/)\""
  },
  "dependencies": {
    "electron": "^43.4.1"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0"
  }
}
