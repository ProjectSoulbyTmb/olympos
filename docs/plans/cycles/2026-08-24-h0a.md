# Olympos cycle — 2026-08-24 late: H0a tier-aware gates

**Scope:** land the respecified H0a precondition (fleet visibility &
gate safety) so tier-3 satellites can be admitted without being able
to redden the fleet.

## What landed

`sentinel.py` now routes on `tier >= 3` for ALL registry rows:

- gate definitions carry the realm's tier; results are tagged with it
- **godot-template's dual registration removed** (registry row owns it;
  the hardcoded blocking entry is gone) — the existing T3 contract
  violation named in the 1042 review is fixed as a side effect
- T3 failures: ledger payload carries `severity:"informational"`,
  and they are **excluded from `failed[]`, the summary counts and the
  exit code** (severity tagging alone would still exit 1 — the exact
  MED finding #3 from the review)
- summary line gains a separate non-blocking segment:
  `T3 informational: n/m green (red, non-blocking: …)`
- dry-run drill: `SENTINEL_DRILL_T3=1` injects a guaranteed-red T3
  stub gate for repeatable proof
- `sentinel.py --doctor`: probe list derived from the registry
  (hardcoded `(43901, 43903)` pins removed) with tier-aware listener
  classification — a busy T3 port logs
  *"companion running (informational)"* instead of warn/squatter;
  T1/T2 owned ports must still be free at rest

## Verification (per roadmap checklist)

- `--list` marks `[T3 informational]` on godot-template / riley /
  riley-studio; no duplicate godot entry
- drill run with forced T3 red: **exit 0**, `35/35 gates green`,
  ledger line carries `severity:"informational"`
- live satellites: `sentinel --doctor` with aphrodite (:43904)
  running logs `OK port 43904 (T3) companion running`; exit 0
- `doctor.py --ci` STABLE before and after (realm results unchanged);
  `verify_system` 4/4 (ledger lint accepts severity field);
  full-mode doctor STABLE 20/20

## heart-tree question — answered by evidence

`heart/package.json` and `heart/.git` are both absent: the tree is
**not present**, confirming rev 2. Restoration from heart's own
remote is a prerequisite for heart's registry row and remains with
the heart lane / operator. H0a itself was always fleet-side and is
now complete.

## Unrelated incidents tonight (recorded, NOT resolved here)

1. **`D:\new` mass-loss ransom alarm (10816 → 3074 files, 23:58)** —
   real library root, cause not attributable from evidence available
   (APHRODITE culling vs media-lane reorganization). Alarm left SET,
   relaunches FROZEN per doctrine; requires human attribution before
   `--clear-alarm`.
2. **v1.3.0 studio deploy eaten by guardian vault** — the riley lane
   deployed v1.3.0 to `D:\riley` without `--promote`, so INTEGRITY
   reverted it to the vaulted v1.2.0 bytes. Studio manually restored
   single-instance on :43907 (currently v1.2.0). The riley lane must
   redeploy and run `persephone --promote riley`.
