# Cycle 2026-08-24-1158 — APHRODITE P2 (thumbnails, EXIF, favorites)

**Trigger:** operator order "keep building" → execute roadmap P2 on top of
the verified v0.1.0 base.

## Shipped

| Feature | Implementation | Evidence |
|---|---|---|
| Real thumbnails | Windows GDI+ bound via ctypes (`GdiThumbner`); EXIF orientation applied pre-scale; cache `%LOCALAPPDATA%\APHRODITE\thumbs-<roothash>\`; decode-failure falls back to original bytes | gate: `gdi-thumb` mode; smoke: 32.75 MB GIF **0.548 s cold → 0.005 s warm** (~100×) |
| Mini-EXIF panel | Pure-python JPEG APP1/TIFF walker (IFD0 + ExifIFD): make/model/taken/exposure/f-number/ISO/focal/pixels/software; defensive `{}` on anything odd | raw-byte probe: stripped download JPG correctly reports `{}` (no APP1 present, offset −1) |
| ★ Favorites | Only writable surface: `POST/DELETE /api/fav`; app-state JSON in LOCALAPPDATA keyed by root-hash; dead entries pruned on list; UI star + `K` key + sidebar smart folder | gate round-trip PASS; live add/list/remove on path with spaces+parens |

## Constraint notes

- Library remains byte-read-only; PUT/PATCH blanket 405; POST/DELETE routed
  exclusively to `/api/fav`. This is a *narrowed* constraint refinement of
  "read-only", logged here deliberately (fleet rule: label constraint changes).
- Containment extended to both new endpoints (`thumb`, `fav`) — same
  `MediaLibrary.checked()` chokepoint.

## Verification

- `python verify_aphrodite.py` → **26/26 PASS** (18 prior + 8 new).
- Live vs `D:\new`: thumb gen/cache timings above; favorites round-trip;
  top-level dir count unchanged at 26 → **zero library pollution**.
- Bug fixed during gate: module-level `_JPEG_CLSID` used before `import
  ctypes` → NameError caught by suite on first run, moved import to module
  scope. Gate did its job.

## Drift observed during this cycle

- Codex SKILL.md was rewritten by a concurrent writer mid-session
  (new "Knowledge organ"/webstudio section); my earlier aphrodite port
  addendum was lost in the merge. Re-applied as dated addendum.
  Lesson candidate: codex edits must re-read immediately before writing;
  concurrent writers need a merge convention.

## Open questions (operator only)

1. Registry row still pending (carried over).
2. Rogue `:43906` listener (finding from cycle 1138) still unexplained.
3. First-browse thumbnail warm-up: acceptable to generate lazily on scroll,
   or add a `--warm` pre-pass that walks `/api/all` once at startup?

## Next actions

1. Operator: relaunch via bat; judge grid speed + info-panel EXIF on real photos.
2. @hermes: commit `aphrodite/` + `verify_aphrodite.py` when approved.
3. P3 backlog unchanged (PyInstaller packaging, grid virtualization if needed).
