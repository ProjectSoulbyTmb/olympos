# Cycle log — 2026-08-24 09:59 (ad-hoc: "design HEART to the fullest capabilities, learn from mistral.ai")

## Findings (evidence-cited)

1. **HIGH — HEART is real but undeclared.** `heart/` v0.2.0 exists as a
   self-managed nested repo (`2fec27e` "deskmate joins…"; `git status`: `?? heart/`).
   Absent from DESIGN.md ecosystem table, STRATEGY.md tiers, and
   `realms/registry.json` (9 rows audited). Port 4767 unprotected by squatter
   check; `npm test` surface aggregated by nothing. Violates the registry-as-
   single-source intent and STRATEGY gaps #1/#3.
2. **MED — Codex drift.** athena-codex §2 says "Still missing: DESIGN.md";
   disk has DESIGN.md (109 lines, decision log current through 2026-08-24).
   Disk wins; codex §2 needs revision (top NEXT ACTION).
3. **LOW — INTEGRATION.md internal lag.** §1 data-plane row still cites
   :43590/:43591; §2 port table lists only vulcan while disk registry has
   zeus 43902 + ptah 43903 too. Doc-internal inconsistency, no behavior impact.
4. **INFO — Concurrent lanes in flight:** modified `hermod/__init__.py`,
   `hypnos/content.py`, `safeguards/verify_safeguards.py`; new untracked
   `hebe/` (contains only `verify_hebe.py`). Untouched this cycle.

## Mistral lessons adopted (mistral.ai, retrieved this cycle)

Layered product stack → HEART Core/Studio/Skills/Horizon layers; Vibe's
long-horizon feature set (scheduling, persistent memory, reusable skills,
knowledge search) → H3/H4; "prompts & skills need a system of record" →
content-addressed skills ledger (H2); model tiering → small-first local brain
ladder; Studio evals/judges/guardrails + Shieldstral → lib/judge.js (H5);
sovereignty/self-hosting → already HEART doctrine, locked as ADR commitment C8.

## Decisions

- **ADR-0001 (Proposed): layered sovereignty.** HEART stays a T3 satellite
  with declared identity (registry row, informational gate), additive wire
  alignment (`error` on every response), fleet patterns adopted internally
  (quarantine, content addressing, fail-open), opt-in bus bridge only.
  Option B (immediate T2) rejected: wrong cadence class for pulse/envelope
  ceremony; poor reversibility.

## Artifacts produced

1. `docs/adr/0001-heart-layered-sovereignty.md`
2. `docs/plans/heart-roadmap.md` (H0–H5 + risk register + T3→T2 promotion checklist)
3. `docs/contracts/heart-interface-v1.md` (envelope, skills manifest, horizon plan, bus letter, rights map)

Budget respected: 3 artifacts + this log. No code touched; no gates weakened;
no secrets written.

## Open questions (operator-only)

- Confirm HEART should remain MIT/nested-repo autonomous vs. being absorbed
  into the parent repo at promotion time (affects checklist step 5 wording).
- Is `hebe/` another agent's lane? Left untouched; confirm ownership before
  any future cycle references it.
- Appetite for the catalogue row `vitals.heart` (INTEGRATION §6 edit) now vs.
  when H5 lands.

## Next actions (ordered)

1. **ATHENA (next cycle or immediate):** update athena-codex §2 — DESIGN.md
   exists (cite today's audit date); add heart/hebe to known-disk watchlist.
2. **reviewer:** critique ADR-0001 + H0's sentinel tier-awareness precondition
   (touches gate derivation = guarantee #5 surface). Requested via ADR header.
3. **hermes (after reviewer sign-off + operator yes on H0):** land H0a
   (sentinel tier routing) then H0 registry/doc rows, per FLOW.md PR lane.
4. **heart maintainer (human or build loop):** implement H1–H2 against
   docs/contracts/heart-interface-v1.md; each phase its own squash PR.
