# Cycle log — 2026-08-24 10:08 (ad-hoc continuation: "add more" on HEART design)

## Scope

Second pass on the HEART design. Prior cycle's ADR/roadmap/contract remain
authoritative; this cycle deepened capability coverage rather than revisiting
decisions. Budget: 1 new artifact + 2 revisions of prior artifacts + this log.

## Findings

1. Design gap from cycle 1: two Mistral lessons were mapped but not yet
   specified — Voxtral (speech) and Studio observability (analytics). Both now
   have phases (H6/H7) with zero-dep translations.
2. No injection-surface audit existed for planned UI features; widget system
   (H8) identified as the only HTML-adjacent risk → hard rule added
   (id allowlist, text/SVG-only rendering), Medium risk flagged for @reviewer.
3. Privacy model was implicit across ADR commitments; now explicit in the
   atlas (data residency, network surface, buslink minimality, honest limits).
4. Hygiene items surfaced by the new design: EADDRINUSE UX, log rotation
   before insights land — recorded in atlas backlog.

## Work produced

- **New:** `docs/plans/heart-capabilities.md` — 28-row capability matrix
  (shipped/phased/stretch/non-goal), mood engine spec, voice architecture
  sketch, privacy & threat model, hygiene backlog, sequencing rule.
  Includes 4 formal non-goals (accounts/cloud, telemetry, OCR import,
  model training) + multi-user.
- **Revised:** `docs/plans/heart-roadmap.md` — appended H6 (voice),
  H7 (insights), H8 (widgets), H9 (deep-work DND flag) with full
  goal/files/verification/acceptance/rollback/risk blocks; +5 risk rows.
- **Revised:** `docs/contracts/heart-interface-v1.md` — §6 voice settings +
  deterministic mood enum; §7 /api/insights schema incl. error path;
  §8 /api/widgets layout schema incl. corrupt-layout path; §9 advisory DND
  flag contract for consumers.

## Decisions

- Voice runs client-side via platform SpeechSynthesis; gating math pure in
  `lib/voice-policy.js`; failure mode is silence, never an error.
- Widgets are declarative + allowlisted; no user HTML passthrough ever.
- DND coordination is an advisory file handshake, default-off; consumers
  defer, never drop; expiry mandatory.
- Four capabilities formally declared non-goals (reversal requires logged
  decision per doctrine).

## Open questions (operator-only)

- None new; cycle-1 questions stand (nested-repo vs absorption, hebe/
  ownership, vitals.heart catalogue timing). H8's injection surface added to
  the reviewer request alongside ADR-0001.

## Next actions

1. **@reviewer:** critique surface now includes H8 widget allowlist design.
2. **hermes/lane owner:** unchanged from cycle 1 (H0a→H0 after sign-off).
3. **ATHENA next cycle:** codex §2 update still pending (DESIGN.md exists);
   consider folding heart capability matrix reference into codex watchlist.
