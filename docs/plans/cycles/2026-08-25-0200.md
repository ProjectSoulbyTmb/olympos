# Cycle log — 2026-08-25 ~02:00 (stabilization: permanent fixes)

Trigger: operator ordered "create all permanent fixes for this to create
stability" following the sovereign-workshop plan. Explicit implementation
authorization; stability scope only (W0 + W1); reviewer-gated W2/W4 untouched.

## Fixed (evidence-cited)

1. **CI runner host — toolcache marker.** Root cause of the ≤5s infra deaths
   then setup-python throws: `actions/setup-python@v5` re-ran its installer
   every run because `D:\actions-runner\_work\_tool\Python\3.12.10\x64.complete`
   was missing while a working 3.12.10 sat in cache; installer step died on
   HKLM registry cleanup (unelevated service). Wrote the zero-byte
   `x64.complete` marker (host-side fix, no commit needed).
   Superseded-in-part upstream: `54a336a` (#82) dropped setup-python entirely;
   `2744dfa` fixed verify_coverage trace-blindness (the real cause of the
   transient 0% coverage readings I saw mid-surgery).
2. **Main divergence resolved.** Rebased H0a (`23128e2`→`942fe44`) onto
   origin; upstream now carries it (`942fe44` visible in origin/main log).
   L034 landed upstream → next lesson id is **L035** (collision avoided).
3. **Poseidon self-healing landed** on `auto/poseidon-selfheal` (`f111889`,
   6 files): berth rebuild, index sweep, ledger tail trim, quarantine probes,
   push adoption+replay. Gate 18/18 pre-commit. DESIGN.md + README rows ride
   the branch.
4. **Sovereign workshop decision record landed** on
   `auto/athena-sovereign-workshop` (`1f8961f`): ADR-0002, roadmap,
   first cycle log.
5. **Daedalus W1 isolation hardening** on `auto/daedalus-w1-isolation`
   (`2f07f94`): `_bind()` fails loud on port capture (no more silent
   `port += 1` drift from registered :43905); optional caller `who` rides the
   witness attestation vector (`[who, *args]` digested — provenance without
   touching T1 norn). Gate **17/17** including two new checks:
   `port_capture_fails_loud_never_drifts`, `caller_identity_rides_the_witness_line`.
6. **Local fleet STABLE:** `py -3 doctor.py --ci` → **20/20 suites green**
   post-rebase (twice).

## Environment findings (new, recorded for the vault)

- Interpreters mutated live mid-session: bare `python` alternated between
  Store-stub and real; `py` launcher lost registrations; toolcache python.exe
  vanished during my commit window. Cause: concurrent provisioning lane
  installed **`D:\Python312`** (01:46) and wired runner `.env`
  (`{PATH} prepend D:\Python312`). Lesson candidate **L035**: "Pin interpreters
  by absolute path in gates/hooks; PATH-based resolution breaks under live
  reprovisioning." Safeguards pre-commit hook invokes bare `python` — broke
  twice today; candidate fix: hook resolves `sys.executable`-style fallback.
- **Concurrent lane active throughout** (L025 respected): landed #82/#81/
  L034/`2744dfa`, pushed our rebased H0a to origin, provisioned D:\Python312,
  and holds IN-FLIGHT uncommitted fixes for the two remaining remote reds —
  `hypnos/content.py +5` registers artemis/mind in BUILD_GATES (clears the
  autopilot contract fail), `haven/build_haven_db.py +42` +
  `haven/canon_fingerprint.py` + `riley-studio/canon.py` are the canon lane's
  work (related to remote haven "46 topics built" fail). `M doctor.py` in-tree
  = tier-aware squatter map companioning H0a. None touched by this cycle.
- Remote main run 32817238340: **daedalus gate PASS 133.9s** — yesterday's
  flake is dead on self-hosted infra. Remaining remote reds: autopilot
  registration (their +5 fixes), haven build assertion (their lane),
  assistant-job `bash: command not found` (runner PATH, #82 follow-up).
- Drift finds for @argus: `mind/verify_mind.py` exists but has NO
  realms/registry row; codex §2 port list omits aphrodite/harmonia/riley/
  haven rows present on disk; cycle-1310 "aphrodite-free" prose contradicts
  disk.

## Open questions (operator)

1. Runner redundancy still single-box (`earth949-d`) — survival posture?
2. May I take the ci.yml assistant-job bash fix, or does the CI lane own it?

## Next actions

1. @hermes — push `auto/poseidon-selfheal`, `auto/daedalus-w1-isolation`,
   `auto/athena-sovereign-workshop`; open squash PRs (bodies cite gate runs).
2. canon/haven lane — land BUILD_GATES +5 and haven DB work; clears last reds.
3. @argus — registry row for `mind/`; codex port-list refresh.
4. @metis — mint L035 (interpreter pinning) + consider lesson: "toolcache
   .complete markers" (now historical, superseded by #82).
5. After green sweep on main: reviewer-gated W2 (intake) may start per roadmap.

CYCLE: stabilization ad-hoc · commits f111889 / 1f8961f / 2f07f94 · stop.
