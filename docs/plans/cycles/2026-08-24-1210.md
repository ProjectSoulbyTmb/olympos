# Cycle log - 2026-08-24 12:10 (execution: constant ecosystem patches)

## Patches shipped

1. **PR #56** - safeguards/gate.py keeps failed-suite output (L030 enacted):
   bounded 8KB capture into report.json, TIMEOUT now preserves partial
   stdout, human summary prefers the suite's own [FAIL]/traceback line via
   _fail_detail().
2. **PR #58** - contract test gate_orchestrator_parallel_timeout_report
   adopts the extended report shape (output key; green suites stay quiet).
   This un-red the ecosystem job: **first full CI green since 14:27Z**
   (ecosystem PASS 3m47s, daedalus included).

## Release

VERSION 1.13.0 -> 1.14.0. Contents: heart design suite rev2 (ADR-0001,
roadmap H0-H9, interface v1, capability atlas), lessons L029-L031,
post-merge verify-and-remediate ship step (#49), gates evidence retention
(#56/#58), codex disk-truth corrections.

## Standing loop

sentinel --watch cadence proposed as the constant patch heartbeat; launch
command and stop-command documented in the cycle transcript.

## Verification trail this pass

- synthetic failing probe surfaced culprit line via new _fail_detail()
- contract test green in isolation (importlib path identical to CI static)
- ecosystem job green on #58 run 32755406916
