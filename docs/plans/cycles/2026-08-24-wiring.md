# Olympos cycle — 2026-08-24 evening wiring pass

**Scope:** complete cross-organ wiring per INTEGRATION §6; close the
observability gaps found on the way.

## What landed (commits 78f70d9..13c0ec0)

1. **Bus catalogue constants** — `TOPIC_*`/`KIND_*` now live in
   `ratatosk/bus.py` as the importable single source.
2. **Registry v2 at 29 members** — publishes/consumes annotated;
   satellites riley `:43907` / aphrodite `:43904` declared;
   harmonia re-based to `:43908` (port collision with live RILEY);
   `riley-studio/` declared as its own Electron+ComfyUI product.
3. **Live wires** — Hades broadcasts `provenance.seal` per seal
   (`artifacts.sealed`); GAIA publishes per-member samples
   (`vitals`, Node side, CLI call-site keeps tests hermetic); relay
   announces `fleet.render-done` for studio jobs submitted outside
   the order stream (monotonic j<N> cursor).
4. **PERSEPHONE fixes** — history ledger bootstraps (was silently
   dead since inception), INTEGRITY restores recorded with file
   lists, verify suite fully state-isolated, README state-root truth.
5. **Satellite gate** — `verify_riley_satellite.py`: skip-green when
   undeployed/dark, contract check when live, `--deep` runs the
   satellite's full suite (104 checks at time of writing).
6. **Knowledge** — L035–L038 minted; engineering rules 22–25 added.

## Incidents during this cycle

- Mirror clone (`D:\Default Project`) had drifted 69 committed files
  behind the workspace — the execution copy for scheduled tasks was
  running stale kernels. Root cause: no mechanism reminds a lane to
  sync after committing.
  **Fix:** `mirror_sync.py` + `safeguards/githooks/post-commit`; every
  commit now converges the mirror automatically, and
  `mirror_sync --audit --strict` is available as a CI-able gate.

## Gates

ratatosk 28/28 · relay 17/17 · hades 11/11 · gaia 18/18 ·
harmonia 22/22 · doctor STABLE 20/20 · satellite deep suite green.
