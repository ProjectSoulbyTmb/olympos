# Cycle log — 2026-08-24 10:42 (ad-hoc continuation: @reviewer critique of the HEART suite)

## Scope

Execute the review step left pending by cycles 1–2 before any H0 landing.
Read-only review via the reviewer role, then fold accepted findings into
the four design artifacts as rev 2. Budget respected: doc revisions + this
log; no code touched; no gates weakened.

## Review outcome

Verdict: **REQUEST-CHANGES** — 10 findings (1 critical-evidence, 2 HIGH,
5 MED, 2 INFO). All ten **accepted** and folded in:

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | HIGH | Evidence base absent/stale: `heart/` empty on disk (no `.git`); "9 realms" stale (registry = 20) | Verified on disk by ATHENA (`Test-Path heart\.git` → False; count → 20). ADR evidence-status note added; all phases gate on `Test-Path heart\package.json`; counts corrected (post-insert expectation 21). Root cause recorded: tree lived only in an untracked nested repo; `2fec27e` added only `.gitignore` rows |
| 2 | HIGH | H5 bus letter conformant to neither real format (`envelope.py` exact-match TOPICS; `bus.broadcast()` record shape) | Contract §4 rewritten: publish via `bus.publish("vitals.heart", …, kind="vital")`; actual stored shape `{v,topic,seq,from,kind,ts,epoch,payload}` documented; non-conformant sample superseded |
| 3 | MED | H0a underspecified; `godot-template` already tier-3 blocking today (existing T3-contract violation); severity tag alone still exits 1; dual-registration hazard | H0a respecified: route on `tier>=3` for all rows; exclude from `failed[]`/summary/exit code; dry-run drills for heart AND godot-template; no dual registration |
| 4 | MED | Port story wrong (`doctor.py` OWNED_PORTS already registry-derived) + busy companion port ⇒ permanent warn/exit-2 alarm fatigue | ADR C1 + H0 evidence corrected; remaining work rescoped to sentinel probe-list derivation + tier-aware listener classification (T3 busy ⇒ informational); verification moved to full-mode doctor |
| 5 | MED | Electron shell unhardened — DOM injection becomes Node RCE | Contract §8 Electron mandate (webPreferences flags, CSP, startup self-test); H8 files/verification extended |
| 6 | MED | Loopback treated as trust boundary — DNS rebinding + cross-origin POST/beacon reach L1 verbs | Threat model expanded; H1 gains Host-header allowlist + Origin/per-boot-token checks with rejection drills |
| 7 | MED | SVG mandate under-specified — attribute-borne vectors survive text escaping | Mandate tightened: `createElementNS`+`textContent` only, zero attribute interpolation; payload corpus extended with SVG-borne cases incl. network-request asserts |
| 8 | LOW | C2 unknown-field rejection is a behavior change vs silent-ignore | Labeled deliberate change with ship-order (UI stops sending legacy keys first, or schema_version-gated) |
| 9 | INFO | GAIA auto-discovers root-level nested repos; invisibility claim overbroad | Claim scoped to docs/registry/gates; H0 verification notes GAIA delta check |
| 10 | INFO | Rights vocabulary fuzziness (norn profiles vs registry roles) | Vocabulary note in contract §5; flagged for promotion checklist |

Reviewer affirmative finding worth keeping: the suite's determinism,
quarantine, atomic-write and rights-posture designs preserve guarantees
#1–#4; guarantee #5 pressure is confined to H0 — which is exactly what
findings 1/3/4 fix.

## Decisions

- ADR-0001 stays **Proposed**: review is folded but operator sign-off on
  H0 remains outstanding (per autonomy bounds, shared-gate changes wait
  for a human yes).
- No code changes anywhere in this cycle; sentinel/doctor work belongs to
  the hermes lane per cycle-1 ordering.

## Open questions (operator-only, unchanged + one new)

- Cycles 1–2 questions stand: nested-repo vs absorption at promotion;
  hebe/ ownership; vitals.heart catalogue timing.
- **New:** where did the heart source tree go? It was present at the
  morning audit, absent by 10:30. If it was moved deliberately, say where;
  if not, it needs restoring from its own remote/backup before H0.

## Next actions

1. **Operator:** answer the heart-tree question + give/deny the H0 go.
2. **hermes/lane owner (after both):** land H0a exactly as respecified
   (tier routing incl. exit-code exclusion; godot-template regression
   drill), then H0 registry/doc rows — PR lane per FLOW.md.
3. **heart maintainer (after restore):** re-verify morning-audit line-level
   claims (heart.js:33 bind, config.js boolean-revert bug, studio.html
   innerHTML sink) against the restored tree; then H1–H2 per contract v1.
