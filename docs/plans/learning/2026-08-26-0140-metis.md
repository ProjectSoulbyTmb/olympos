# METIS learning cycle - 2026-08-26 01:40

Budget: 5 proposals. Staged: 5. Vault untouched (45 lessons, L001-L045;
promotion belongs to Athena + operator).

## Streams swept

| Stream | State |
|---|---|
| `data/sentinel/incidents.jsonl` | ABSENT (quiet; lazy-create writer never fired in this checkout) |
| `zeus/data/audit.jsonl` | ABSENT (quiet) |
| `data/health_report.json` | LIVE, GREEN: fail 0, warn 0, 20/20 component suites, 84.1s STABLE - no FAIL rows to mine this cycle |
| `docs/plans/cycles/*.md` | LIVE: all 19 logs read end-to-end (08-24 series x13, 08-25 x3, 08-26 x3) |
| `knowledge/lessons.json` | READ for dedupe, 45 lessons |

Evidence weight this cycle came from cycle logs + the soak baseline ledger;
both jsonl streams were quiet, which is itself evidence (see proposal on
silent ledgers).

## Proposals staged (files under knowledge/proposals/, IDs assigned at promotion)

1. **Automated merges wait for required checks to finish green** (autonomy)
   - PR #45 and #49 squash-merged ahead of check completion while main's
     ecosystem job was red; same-day audit found no merge path gated on
     checks. Post-merge remediate mitigates; nothing prevents.
2. **Reconcile monotonic identifiers against merged history before allocating** (data-integrity)
   - Three identifier spaces raced in three days: vault next_id:L034 vs
     upstream-landed L034; duplicate ADR numbers (two 0002s/0003s/0005s);
     Track A/B phase-range collision. Wiring-era L035-L038 mints were
     superseded by different content under the same IDs after fork
     reconciliation - the race bit, not merely threatened.
3. **Pin interpreters by absolute path in gates, hooks and scheduled tasks** (reliability)
   - Bare `python` in the safeguards hook broke twice on 08-25 when a
     provisioning lane landed D:\Python312 and PATH flipped mid-session.
     This lesson was queued as "mint L035" in 0200:75 and silently lost -
     the slot was taken by the hermes dirt lesson. Reminted here.
4. **A clean ledger proves nothing unless the watcher provably ran** (reliability)
   - Both Olympos sentinel homes wrote zero lines during the V5 soak window
     (one file absent, one quiesced since 08-24T16:01:01, before day zero)
     while auto-arm certification reads "zero cross-fleet incidents in
     either ledger". Guarantee-threatening near-miss caught only by the
     Day-1 baseline sweep.
5. **Verify claimed ports by process identity, not port number or name match** (security)
   - :43904 held by Aphrodite.exe (name matches realm, command line does
     not match atlas registration; open escalation), after an unregistered
     :43906 listener the prior day. Two halves of one hole across two days.

## Candidates rejected

- **Rogue commit on mirror checkout** (volt-comm drift #1): instance of
  L025/L027/L031/L033 family; operator adjudication already owns it.
- **Boundary-gate red on literal `D:\VOLTAGE` mentions** (volt-comm drift #2):
  resolved same-cycle in W0 via indirection (thoth-metal-w0:33-41); single
  occurrence, fixed - under-evidenced as a new rule.
- **Missing VOLTAGE verifier scripts in sentinel FAILs** (soak-baseline:42-46):
  dangling verify path is L037's null-verifier class; covered by extension.
- **Watcher `plan_list` rights regression**: single live instance, owned by
  the daedalus/hermes repair lane; L013 covers profile-gating design.
- **Test-Path-after-write in OneDrive dirs** (1138:48-51 queued candidate):
  ONE direct occurrence (server.py write loss); fails the two-corroboration
  bar. Queued candidate that also never minted - flagged in NEXT.
- **D:\new mass-loss alarm / guardian-vault deploy revert** (h0a incidents):
  doctrine worked as designed (L009 quarantine-over-destruction); not a gap.
- **Codex concurrent-writer merge loss** (1158:33-37): covered by L025
  re-read-before-edit + L041/L044 doc-drift family.
- **Stale verify/echo process trio** (soak Day-1 item iii): housekeeping,
  single occurrence.

## Meta-observation for the fleet

Two queued @metis handoffs from prior cycles ("mint L035" interpreter
pinning at 0200:75; Test-Path-after-write at 1138:48-51) died quietly -
one to an ID-slot collision, one to no owner picking it up. The identifier-
reconciliation proposal addresses the first mechanism. Next cycles should
grep queued `@metis` actions in new cycle logs before closing.

MINED: 9 findings -> 5 proposals (staged, IDs at promotion)
REJECTED: most common reason - covered by existing vault lessons
(L025/L027/L031/L033 coordination family; L037 verifier class; L009/L013);
second reason - single occurrence failing the two-corroboration bar.
NEXT: (1) re-check watcher plan_list gate once repaired - if rights-vs-doctrine
drift recurs, mine it; (2) watch for a second OneDrive silent-write-loss
instance to clear the Test-Path-after-write bar; (3) confirm hades-alerts
refusals as drill artifacts - if drill labeling stays absent and triage burns
again, propose envelope drill-markers; (4) verify Aphrodite.exe disposition
and whether process-identity checking lands in sentinel.
